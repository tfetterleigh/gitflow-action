name: 'WIP Gitflow'
description: 'WIP Gitflow'

runs:
  using: 'node20'
  main: 'dist/index.js'

inputs:
  gh-token:
    description: 'GITHUB_TOKEN'
    required: true

branding:
  icon: 'git-pull-request'
  color: 'blue'